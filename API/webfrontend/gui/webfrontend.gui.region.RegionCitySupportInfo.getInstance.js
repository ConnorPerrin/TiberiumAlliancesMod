function setCity(n) {
    this.__bfL = n;
    var i = n.get_SupportWeapon(),
        r = n.get_SupportData();
    this.__bgf = i.r;
    this.__bgb.setValue(this.tr(y, t + i.dn + u, t + r.get_Level() + u, t + r.get_Magnitude() + u));
    this.__bgc.setValue(t + i.pi.toString() + s);
    this.__bgd.setValue(t + i.pv.toString() + s);
    this.__bge.setValue(t + i.pa.toString() + s)
}