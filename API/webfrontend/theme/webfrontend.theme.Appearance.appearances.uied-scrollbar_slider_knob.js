function style(n) {
    var t = n.horizontal ? eit : ig;
    return n.disabled && (t += nr), {
        decorator: t,
        minHeight: n.horizontal ? undefined : 16,
        minWidth: n.horizontal ? 16 : undefined
    }
}