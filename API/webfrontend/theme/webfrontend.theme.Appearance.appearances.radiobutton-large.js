function style(n) {
    var t;
    return t = n.checked && n.focused ? wit : n.checked && n.disabled ? lot : n.checked && n.pressed ? gn : n.checked && n.hovered ? sut : n.checked ? yd : n.disabled ? kb : n.focused ? sk : n.pressed ? att : n.hovered ? kn : hut, {
        icon: ort + t + ct,
        gap: 3
    }
}