function style(n) {
    return {
        decorator: n.focused ? er : n.disabled ? sr : vu,
        minHeight: 26,
        padding: [4, 6],
        textColor: n.disabled ? v : n.showingPlaceholder ? ri : w
    }
}